package Model;

import Utils.Color;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class PackMan extends GameObject{

	public PackMan(int id, int speed, ImageView image, Location currentLocation, Color color) {
		super(id, speed, image, currentLocation, color);
	}
	

}
